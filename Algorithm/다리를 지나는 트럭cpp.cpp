#include <string>
#include <vector>
#include <iostream>
#include <queue>
using namespace std;
queue<int>q;
int solution(int bridge_length, int weight, vector<int> truck_weights) {
    int answer = 0;
    int total = 0;
    int sec = 1;
    q.push(0);
    return answer;
}

int main() {
    cin.tie(NULL);
    std::ios::sync_with_stdio(false);
    return 0;
}